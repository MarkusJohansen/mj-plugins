# Fix plan — __PROJECT_NAME__

Prioritised, top-to-bottom. The loop picks the **first unchecked** item each iteration. Tick items as they're completed. Add new items as you discover them.

If this list goes stale, the next loop's single task is to regenerate it by comparing `src/` against `specs/`.

## Now

- [ ] Have a requirements conversation with the user and write `specs/<feature>.md` files (one per feature, with Goal / Requirements / Non-goals / Acceptance tests).

## Next

<!-- Populated from specs once they exist. -->

## Later

<!-- Nice-to-haves. Don't let these crowd "Now". -->

## Done

<!-- Move ticked items here so "Now" stays scannable. -->
