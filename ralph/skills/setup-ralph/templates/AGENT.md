# AGENT.md — __PROJECT_NAME__

How to operate this repo. Keep this file lean. **Never** use it as a status log.

## Build

<!-- e.g. `npm install && npm run build` — fill in once known -->
_TBD — first loop should populate this._

## Test

- **Focused tests** (run after changing a unit):
  <!-- e.g. `npm test -- <file>` -->
  _TBD_
- **Full validation** (run before committing a non-trivial change):
  <!-- e.g. `npm test && npm run lint && npm run typecheck` -->
  _TBD_

## Run

<!-- How to start the app locally -->
_TBD_

## Rules

- No placeholder implementations. If the spec asks for behaviour X, implement X.
- Search before assuming something is missing. Use parallel subagents.
- Prefer existing modules over new ones. Check `src/` (or equivalent) before adding files.
- Update `fix_plan.md` every loop. Tick what you did; add what you discovered.
- Commit each increment. Small, reversible commits.
